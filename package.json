{
  "license": "MIT",
  "dependencies": {
    "@types/react": "^*",
    "mobx": "^6.13.2",
    "mobx-react-lite": "^*",
    "react": "^*",
    "@js-temporal/polyfill": "^*",
    "unws": "^*",
    "zod": "^*"
  },
  "scripts": {
    "ts-check": "tsc",
    "_test": "vitest --run",
    "test": "deno test  --allow-hrtime --v8-flags=\"--expose-gc\" --trace-leaks"
  }
}
