{
  "dependencies": {
    "@types/react": "^*",
    "mobx": "^*",
    "react": "^*",
    "zod": "^*",
    "mobx-react-lite": "^*"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "scripts": {
    "ts-check": "tsc",
    "test": "node --expose-gc ./node_modules/.bin/vitest --run"
  }
}
