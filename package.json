{
  "license": "MIT",
  "dependencies": {
    "@types/react": "^*",
    "mobx": "^6.13.2",
    "mobx-react-lite": "^*",
    "react": "^*",
    "@js-temporal/polyfill": "^*",
    "unws": "^*",
    "zod": "^*"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@vitest/coverage-v8": "^2.1.1",
    "typescript": "^5.4.5",
    "vitest": "^2.1.1"
  },
  "scripts": {
    "ts-check": "tsc",
    "_test": "node --expose-gc ./node_modules/.bin/vitest --run",
    "test": "vitest --run"
  }
}
